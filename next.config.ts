import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['images.unsplash.com', 'upload.wikimedia.org','randomuser.me','source.unsplash.com','defaultimage.com','projectsly.com'] 
  },
};

export default nextConfig;
